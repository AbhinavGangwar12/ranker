# --- Web framework ---
fastapi
uvicorn[standard]

# --- Agent orchestration ---
langgraph
langgraph-checkpoint-postgres
langchain-core
langchain-groq          # Groq LLM client for LangChain/LangGraph
langsmith                # observability/tracing

# --- Database (async app layer) ---
sqlalchemy
asyncpg                  # async Postgres driver for SQLAlchemy (users/threads/messages)
alembic                  # migrations

# --- Database (LangGraph's own checkpointer — needs psycopg v3, NOT psycopg2) ---
psycopg[binary]
psycopg-pool

# --- Auth ---
passlib[bcrypt]
bcrypt<4.1                 # passlib's backend detection breaks on bcrypt 4.1+ (missing __about__ attr)
python-jose[cryptography]
python-multipart          # needed for OAuth2PasswordRequestForm / form-based login

# --- Config ---
pydantic
pydantic-settings
email-validator            # required for pydantic's EmailStr (used in schemas/auth.py)
python-dotenv

# --- Rate limiting ---
slowapi

# --- CORS is built into FastAPI (starlette.middleware.cors), no extra package needed

# --- Dev / notebook testing ---
jupyter
ipykernel
nest-asyncio              # lets you await async LangGraph/FastAPI calls inside notebook cells